export enum DonationStatus {
  AVAILABLE = 'available',
  FULFILLED = 'fulfilled',
  MATCHING = 'matching',
}

export enum RecourranceEnum {
  ONCE = 'once',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}
