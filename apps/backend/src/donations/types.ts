export enum DonationStatus {
  AVAILABLE = 'available',
  FULFILLED = 'fulfilled',
  MATCHED = 'matched',
}

export enum RecurrenceEnum {
  NONE = 'none',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}

export enum RecurrenceEnum {
  NONE = 'none',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}
