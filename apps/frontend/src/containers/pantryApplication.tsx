import { Center } from '@chakra-ui/react';
import PantryApplicationForm from '@components/forms/pantryApplicationForm';
import React from 'react';

const PantryApplication: React.FC = () => {
  return (
    <Center>
      <PantryApplicationForm />
    </Center>
  );
};

export default PantryApplication;
